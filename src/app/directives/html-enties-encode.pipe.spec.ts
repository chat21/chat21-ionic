import { HtmlEntiesEncodePipe } from './html-enties-encode.pipe';

describe('HtmlEntiesEncodePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlEntiesEncodePipe();
    expect(pipe).toBeTruthy();
  });
});
